
<div class="flex flex-col items-center">
    <div class="flex flex-row items-center">
        <%= image_tag url_for(@profile.image), class: "text-red w-28 h-28" if @profile.image.attached? %>
        <div class="text-red-500"> <%= @profile.name %> </div>
    </div>
    <p class="text-lg"><%= @profile.bio %></p>
</div>